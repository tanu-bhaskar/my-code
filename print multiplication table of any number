#include<stdio.h>
int main()
{
    int i,n,table=1;
    printf("Enter a number whose table you want: ");
    scanf("%d",&n);
    for(i=1;i<=10;i++)
    {
        table=i*n;
        printf("%d * %d = %d",n,i,table);
        printf("\n");
    }
    return 0;
}
